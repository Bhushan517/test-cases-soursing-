export enum WORKFLOW_STATUS {
    PENDING = 'pending',
    COMPLETED = 'completed',
    HALT = 'halt',
    HOLD = 'hold'
}

export enum WORKFLOW_FLOW_TYPE {
    REVIEW = 'Review'
}

export enum JOB_STATUS {
    PENDING_APPROVAL_SOURCING = 'PENDING_APPROVAL_SOURCING',
    SOURCING = 'SOURCING',
    HALTED = 'HALTED',
    HOLD = 'HOLD'
}

export enum USER_TYPE {
    VENDOR = 'vendor',
    DEFAULT = 'default'
}